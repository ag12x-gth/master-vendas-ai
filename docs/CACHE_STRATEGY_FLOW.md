# Fluxo Completo da Estratégia de Cache - Master IA Oficial

## 📋 Índice

1. [Visão Geral](#visão-geral)
2. [Arquitetura de Cache](#arquitetura-de-cache)
3. [TTL Tiers - Faixas de Tempo](#ttl-tiers---faixas-de-tempo)
4. [Fluxo Detalhado por Endpoint](#fluxo-detalhado-por-endpoint)
5. [Exemplos Práticos](#exemplos-práticos)
6. [Diagramas de Fluxo](#diagramas-de-fluxo)
7. [Performance Esperada](#performance-esperada)

---

## Visão Geral

O sistema de cache expandido implementa uma estratégia **tiered** (em camadas) que otimiza performance baseando-se na frequência de mudança dos dados:

- ✅ **21 endpoints com cache** (aumento de 75% na cobertura)
- ✅ **4 tiers de TTL** (Real-Time → Historical)
- ✅ **Multi-tenant security** preservada
- ✅ **Cache keys únicos** por empresa + parâmetros

---

## Arquitetura de Cache

### 🏗️ Componente Principal: `getCachedOrFetch`

**Localização:** `src/lib/api-cache.ts`

```typescript
async function getCachedOrFetch<T>(
  cacheKey: string,      // Chave única do cache
  fetchFn: () => Promise<T>,  // Função para buscar dados
  ttl?: number           // Tempo de vida (opcional)
): Promise<T>
```

### 🔄 Fluxo de Execução

```
┌─────────────────────────────────────────────┐
│  1. Request chega ao endpoint                │
│     GET /api/v1/dashboard/stats             │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│  2. Gera cache key único                     │
│     "dashboard-stats:empresa123:2024-11-20" │
└─────────────────┬───────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────┐
│  3. Verifica se existe no cache              │
│     cache.get(cacheKey)                      │
└─────────────────┬───────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
        ▼                   ▼
    CACHE HIT          CACHE MISS
        │                   │
        │                   ▼
        │          ┌─────────────────┐
        │          │ 4. Executa query │
        │          │    ao banco      │
        │          └────────┬─────────┘
        │                   │
        │                   ▼
        │          ┌─────────────────┐
        │          │ 5. Salva no cache│
        │          │    com TTL       │
        │          └────────┬─────────┘
        │                   │
        └──────────┬────────┘
                   │
                   ▼
        ┌─────────────────────┐
        │ 6. Retorna resultado │
        └─────────────────────┘
```

---

## TTL Tiers - Faixas de Tempo

### 📊 Tabela de TTLs Implementados

| Constante | Valor | Uso | Motivo |
|-----------|-------|-----|--------|
| `REAL_TIME` | 30s | Dados que mudam constantemente | Leads ativos, mensagens recentes |
| `MEDIUM` | 60s | Dados que mudam frequentemente | Dashboard stats atual |
| `ANALYTICS_CURRENT` | 60s | Analytics do dia atual | KPIs em tempo real |
| `ANALYTICS_TIMESERIES_CURRENT` | 120s | Séries temporais atuais | Charts do dia/semana |
| `CONFIG_SEMI_STATIC` | 300s (5min) | Configurações que mudam ocasionalmente | Conexões WhatsApp, cadências |
| `ANALYTICS_HISTORICAL` | 600s (10min) | Analytics históricos (>1 dia) | KPIs passados |
| `CONFIG_STATIC` | 900s (15min) | Configurações raramente alteradas | Templates, personas IA |
| `ANALYTICS_TIMESERIES_HISTORICAL` | 900s (15min) | Séries temporais históricas | Charts de meses anteriores |

---

## Fluxo Detalhado por Endpoint

### 🔥 1. Dashboard Stats - `/api/v1/dashboard/stats`

**TTL:** 60s (MEDIUM)

#### Fluxo Completo:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/dashboard/stats?                            │
│     startDate=2024-10-22&endDate=2024-11-20             │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ AUTENTICAÇÃO                                             │
│ - Verifica sessão JWT                                    │
│ - Extrai companyId do usuário                           │
│   companyId = "empresa-abc-123"                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ GERAÇÃO DE CACHE KEY                                     │
│ cacheKey = `dashboard-stats:${companyId}:${dateRange}`  │
│ Resultado: "dashboard-stats:empresa-abc-123:30d"        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ VERIFICAÇÃO DE CACHE                                     │
│ const cached = cache.get(cacheKey)                      │
└─────────────────┬───────────────────────────────────────┘
                  │
        ┌─────────┴─────────┐
        │                   │
    SE EXISTE           SE NÃO EXISTE
        │                   │
        ▼                   ▼
┌──────────────┐   ┌────────────────────────────────┐
│ RETORNA CACHE│   │ BUSCA NO BANCO DE DADOS         │
│ ⚡ ~5ms      │   │ - Query de aggregação complexa  │
└──────────────┘   │ - Conta leads, mensagens, etc   │
                   │ ⏱️ ~500-1000ms                  │
                   └────────┬───────────────────────┘
                            │
                            ▼
                   ┌────────────────────────────────┐
                   │ SALVA NO CACHE                  │
                   │ cache.set(cacheKey, data, 60s)  │
                   └────────┬───────────────────────┘
                            │
        ┌───────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────┐
│ RESPOSTA JSON                                            │
│ {                                                        │
│   totalLeads: 1234,                                     │
│   activeConversations: 56,                              │
│   ...                                                   │
│ }                                                       │
└─────────────────────────────────────────────────────────┘
```

#### Cenários Práticos:

**Cenário A - Usuário acessa dashboard às 10:00**
```
10:00:00 → Request → Cache MISS → Query DB (800ms) → Salva cache
10:00:01 → Response retornada ao usuário
```

**Cenário B - Usuário recarrega página às 10:00:30**
```
10:00:30 → Request → Cache HIT → Retorna cache (5ms)
10:00:30 → Response retornada ao usuário (160x mais rápido!)
```

**Cenário C - Cache expira às 10:01:00**
```
10:01:00 → Request → Cache MISS → Query DB novamente
```

---

### 📊 2. Analytics KPIs - `/api/v1/analytics/kpis`

**TTL:** 60s (atual) / 600s (histórico) - **Condicional**

#### Fluxo com Lógica Condicional:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/analytics/kpis?                             │
│     startDate=2024-10-01&endDate=2024-10-31             │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ DETERMINAR SE É HISTÓRICO OU ATUAL                      │
│                                                          │
│ const daysDiff = differenceInDays(                      │
│   new Date(),           // Hoje: 2024-11-20            │
│   new Date(endDate)     // End: 2024-10-31             │
│ )                                                       │
│                                                          │
│ daysDiff = 20 dias → É HISTÓRICO (>1 dia)              │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ ESCOLHA DO TTL BASEADO EM IDADE DOS DADOS               │
│                                                          │
│ if (daysDiff > 1) {                                     │
│   ttl = 600s // Dados históricos mudam raramente       │
│ } else {                                                │
│   ttl = 60s  // Dados atuais mudam constantemente      │
│ }                                                       │
│                                                          │
│ Resultado: ttl = 600s (10 minutos)                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY COM DATAS                                      │
│ cacheKey = `analytics-kpis:${companyId}:${dates}`       │
│ "analytics-kpis:empresa-abc:2024-10-01:2024-10-31"      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
        [Segue fluxo padrão de cache]
```

#### Comparação de Cenários:

**📅 Consultando Dados de HOJE (2024-11-20)**
```
endDate = 2024-11-20
daysDiff = 0 dias
TTL = 60s (dados mudam frequentemente)

10:00 → Query DB → Cache por 60s
10:00:30 → Cache hit
10:01 → Cache expired → Query DB novamente
```

**📅 Consultando Dados de OUTUBRO (2024-10-31)**
```
endDate = 2024-10-31
daysDiff = 20 dias
TTL = 600s (dados históricos estáveis)

10:00 → Query DB → Cache por 10 minutos
10:05 → Cache hit
10:09 → Cache hit
10:10 → Cache expired → Query DB novamente
```

**🎯 Benefício:** Dados históricos são cacheados 10x mais tempo, reduzindo ~90% das queries!

---

### 📈 3. Analytics Timeseries - `/api/v1/analytics/timeseries`

**TTL:** 120s (atual) / 900s (histórico)

#### Fluxo com Granularidade:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST COM GRANULARIDADE                                │
│ GET /api/v1/analytics/timeseries?                       │
│     startDate=2024-10-01                                │
│     endDate=2024-10-31                                  │
│     granularity=day                                     │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY INCLUI GRANULARIDADE                          │
│ cacheKey = `analytics-timeseries:${companyId}:          │
│             ${startDate}:${endDate}:${granularity}`     │
│                                                          │
│ Resultado:                                              │
│ "analytics-timeseries:empresa-abc:                      │
│  2024-10-01:2024-10-31:day"                            │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
        [Lógica condicional igual a KPIs]
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE SEPARADO POR GRANULARIDADE                        │
│                                                          │
│ Cache 1: "...:day" → TTL 900s                           │
│ Cache 2: "...:week" → TTL 900s (diferente!)            │
│ Cache 3: "...:month" → TTL 900s (diferente!)           │
│                                                          │
│ Cada granularidade tem seu próprio cache!               │
└─────────────────────────────────────────────────────────┘
```

#### Exemplo de Múltiplos Caches Simultâneos:

```
Usuário visualiza gráfico de OUTUBRO com diferentes granularidades:

┌──────────────────────────────────────────────────────┐
│ Request 1: granularity=day                            │
│ Cache Key: "analytics-timeseries:...:day"            │
│ Cache MISS → Query DB → Retorna 31 pontos (1/dia)   │
│ ⏱️ 1200ms                                            │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ Request 2: granularity=week                           │
│ Cache Key: "analytics-timeseries:...:week"           │
│ Cache MISS → Query DB → Retorna 5 pontos (1/semana) │
│ ⏱️ 800ms                                             │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ Request 3: granularity=day (novamente)                │
│ Cache Key: "analytics-timeseries:...:day"            │
│ Cache HIT → Retorna cache → 31 pontos                │
│ ⚡ 5ms (240x mais rápido!)                           │
└──────────────────────────────────────────────────────┘
```

---

### 🎯 4. Analytics Funnel - `/api/v1/analytics/funnel`

**TTL:** 120s

#### Fluxo com Filtro de Board:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST COM BOARD OPCIONAL                               │
│ GET /api/v1/analytics/funnel?boardId=board-123          │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY COM BOARD ID                                   │
│ const boardId = searchParams.get('boardId')             │
│ cacheKey = `analytics-funnel:${companyId}:              │
│             ${boardId || 'all'}`                        │
│                                                          │
│ Resultados possíveis:                                   │
│ - "analytics-funnel:empresa-abc:board-123"              │
│ - "analytics-funnel:empresa-abc:all"                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
        [Fluxo padrão de cache com TTL 120s]
```

#### Cenário Multi-Board:

```
Empresa tem 3 boards de Kanban:

┌──────────────────────────────────────────────┐
│ Board 1: "Novos Leads"                        │
│ Cache: "analytics-funnel:empresa:board-1"    │
│ Dados: [Novo→Contato→Proposta→Ganho]        │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ Board 2: "Clientes Ativos"                   │
│ Cache: "analytics-funnel:empresa:board-2"    │
│ Dados: [Ativo→Suporte→Renovação]            │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ Todos os Boards                               │
│ Cache: "analytics-funnel:empresa:all"        │
│ Dados: Agregado de todos os boards           │
└──────────────────────────────────────────────┘

Cada board tem seu próprio cache independente!
```

---

### 📋 5. Message Templates - `/api/v1/message-templates`

**TTL:** 900s (CONFIG_STATIC)

#### Fluxo com Múltiplos Filtros:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST COM FILTROS                                      │
│ GET /api/v1/message-templates?                          │
│     connectionId=conn-123                               │
│     status=approved                                     │
│     search=boas-vindas                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY COM TODOS OS FILTROS                          │
│ cacheKey = `message-templates:${companyId}:             │
│             ${connectionId || 'all'}:                   │
│             ${status || 'all'}:                         │
│             ${search || ''}`                            │
│                                                          │
│ Resultado:                                              │
│ "message-templates:empresa-abc:conn-123:                │
│  approved:boas-vindas"                                  │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ QUERY DRIZZLE COM FILTROS DINÂMICOS                     │
│                                                          │
│ let query = db.select()                                 │
│   .from(messageTemplates)                               │
│   .where(eq(messageTemplates.companyId, companyId))    │
│                                                          │
│ if (connectionId)                                       │
│   query.where(eq(...connectionId))                     │
│                                                          │
│ if (status)                                             │
│   query.where(eq(...status))                           │
│                                                          │
│ if (search)                                             │
│   query.where(like(...search))                         │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
        [Cache por 15 minutos]
```

#### Matriz de Caches Diferentes:

```
Combinações possíveis de filtros = Caches independentes

┌─────────────────────────────────────────────────────┐
│ Filtro 1: Sem filtros                                │
│ Key: "message-templates:empresa:all:all:"            │
│ Retorna: TODOS os templates (50 templates)          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ Filtro 2: connectionId=conn-123                      │
│ Key: "message-templates:empresa:conn-123:all:"       │
│ Retorna: Templates da conexão específica (10)       │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ Filtro 3: status=approved                            │
│ Key: "message-templates:empresa:all:approved:"       │
│ Retorna: Templates aprovados (30 templates)         │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ Filtro 4: search=boas-vindas                         │
│ Key: "message-templates:empresa:all:all:boas-vindas" │
│ Retorna: Templates com "boas-vindas" no nome (2)    │
└─────────────────────────────────────────────────────┘

Cada combinação = Cache independente por 15 minutos!
```

---

### 🤖 6. IA Personas - `/api/v1/ia/personas`

**TTL:** 900s (CONFIG_STATIC)

#### Fluxo Simples:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/ia/personas                                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY SIMPLES (SEM FILTROS)                         │
│ cacheKey = `ia-personas:${companyId}`                   │
│ "ia-personas:empresa-abc-123"                           │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ QUERY DRIZZLE                                            │
│ db.query.aiPersonas.findMany({                          │
│   where: eq(aiPersonas.companyId, companyId),           │
│   orderBy: desc(aiPersonas.createdAt)                   │
│ })                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ RETORNA LISTA DE PERSONAS                               │
│ [                                                        │
│   {                                                     │
│     id: "persona-1",                                    │
│     name: "Atendente Vendas",                          │
│     model: "gpt-4",                                    │
│     systemPrompt: "Você é um atendente...",            │
│     useRag: true                                       │
│   },                                                    │
│   {                                                     │
│     id: "persona-2",                                    │
│     name: "Suporte Técnico",                           │
│     ...                                                │
│   }                                                     │
│ ]                                                       │
│                                                          │
│ Cache válido por 15 minutos                             │
└─────────────────────────────────────────────────────────┘
```

#### Por que TTL Longo (15min)?

```
Personas de IA raramente são modificadas:

┌────────────────────────────────────────────┐
│ Ação do Usuário         │ Frequência       │
├─────────────────────────┼──────────────────┤
│ Criar nova persona      │ 1x por semana    │
│ Editar persona          │ 2-3x por semana  │
│ Listar personas         │ 100x por dia     │
└────────────────────────────────────────────┘

Ratio: 100 leituras / 3 escritas = Cache MUITO eficiente!

Se cache de 15min:
- Leitura 1 → Query DB
- Leituras 2-100 → Cache hit (99% hit rate!)
```

---

### 🔌 7. Connections - `/api/v1/connections`

**TTL:** 300s (CONFIG_SEMI_STATIC)

#### Fluxo com Dados Sensíveis:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/connections                                 │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY                                                │
│ cacheKey = `connections:${companyId}`                   │
│ "connections:empresa-abc-123"                           │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ QUERY COM DADOS ENCRIPTADOS                             │
│ db.select({                                             │
│   id, companyId, config_name,                          │
│   accessToken,  // ⚠️ Retorna ENCRIPTADO               │
│   appSecret,    // ⚠️ Retorna ENCRIPTADO               │
│   wabaId, phoneNumberId, isActive                      │
│ })                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE ARMAZENA DADOS ENCRIPTADOS                        │
│ ✅ SEGURO: Tokens permanecem encriptados no cache       │
│ ✅ Não há descriptografia antes do cache                │
│                                                          │
│ Cache TTL: 5 minutos (semi-estático)                    │
└─────────────────────────────────────────────────────────┘
```

#### Por que Semi-Static (5min)?

```
Conexões mudam mais que personas, menos que analytics:

┌──────────────────────────────────────────────────┐
│ Cenário                  │ Frequência           │
├──────────────────────────┼──────────────────────┤
│ Nova conexão WhatsApp    │ 1-2x por mês         │
│ Ativar/desativar conexão │ 1-2x por semana      │
│ Atualizar token Meta     │ 1x por 60 dias       │
│ Listar conexões          │ 50x por dia          │
└──────────────────────────────────────────────────┘

TTL de 5min = Balanço entre freshness e performance
```

---

### 🔄 8. Cadences - `/api/v1/cadences`

**TTL:** 300s (CONFIG_SEMI_STATIC)

#### Fluxo com Relações (Joins):

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/cadences                                    │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY                                                │
│ cacheKey = `cadences:${companyId}`                      │
│ "cadences:empresa-abc-123"                              │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ QUERY DRIZZLE COM RELAÇÕES (WITH)                       │
│ db.query.cadenceDefinitions.findMany({                  │
│   where: eq(...companyId),                              │
│   with: {                                               │
│     funnel: {                                           │
│       columns: { id: true, name: true }                 │
│     },                                                  │
│     steps: {                                            │
│       orderBy: [steps.stepOrder]                        │
│     }                                                   │
│   }                                                     │
│ })                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ ESTRUTURA RETORNADA (NESTED)                            │
│ [                                                        │
│   {                                                     │
│     id: "cadence-1",                                    │
│     name: "Follow-up 21 dias",                         │
│     funnel: { id: "funnel-1", name: "Vendas" },        │
│     steps: [                                            │
│       { stepOrder: 1, offsetDays: 0, ... },            │
│       { stepOrder: 2, offsetDays: 7, ... },            │
│       { stepOrder: 3, offsetDays: 14, ... }            │
│     ]                                                   │
│   }                                                     │
│ ]                                                       │
│                                                          │
│ Cache inclui TODAS as relações nested!                  │
└─────────────────────────────────────────────────────────┘
```

---

### 📊 9. Analytics Campaigns - `/api/v1/analytics/campaigns`

**TTL:** 120s (atual) / 900s (histórico)

#### Fluxo com Múltiplas Queries:

```
┌─────────────────────────────────────────────────────────┐
│ REQUEST                                                  │
│ GET /api/v1/analytics/campaigns?                        │
│     startDate=2024-10-01&endDate=2024-10-31             │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ CACHE KEY + TTL CONDICIONAL                             │
│ cacheKey = `analytics-campaigns:${companyId}:${dates}`  │
│ ttl = isHistorical ? 900s : 120s                        │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ EXECUÇÃO DE MÚLTIPLAS QUERIES NO CACHE                  │
│                                                          │
│ getCachedOrFetch(cacheKey, async () => {                │
│   // Query 1: Métricas de Campanhas                     │
│   const campaigns = await                               │
│     analyticsService.getCampaignMetrics(...)            │
│                                                          │
│   // Query 2: Métricas de Notificações                  │
│   const notifications = await                           │
│     analyticsService.getNotificationMetrics(...)        │
│                                                          │
│   // Retorna ambos os resultados                        │
│   return { campaigns, notifications }                   │
│ })                                                      │
└─────────────────┬───────────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│ ESTRUTURA RETORNADA                                      │
│ {                                                        │
│   campaigns: {                                          │
│     total: 25,                                          │
│     sent: 10000,                                        │
│     delivered: 9500,                                    │
│     read: 7200                                          │
│   },                                                    │
│   notifications: {                                      │
│     total: 150,                                         │
│     pending: 10,                                        │
│     sent: 140                                           │
│   }                                                     │
│ }                                                       │
│                                                          │
│ 🎯 2 queries agregadas em 1 cache hit!                 │
└─────────────────────────────────────────────────────────┘
```

---

## Exemplos Práticos

### 🎬 Cenário Completo: Usuário Acessa Dashboard

```
┌──────────────────────────────────────────────────────┐
│ 09:00:00 - Usuário faz login e acessa /dashboard     │
└──────────────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Dashboard    │ │ Analytics    │ │ Connections  │
│ Stats        │ │ KPIs         │ │ Health       │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
  CACHE MISS       CACHE MISS       CACHE MISS
       │                │                │
  Query DB         Query DB         Query DB
  800ms            600ms            200ms
       │                │                │
  Save cache       Save cache       Save cache
  TTL: 60s        TTL: 60s         TTL: 300s
       │                │                │
       └────────────────┴────────────────┘
                       │
                Response: 1600ms total

┌──────────────────────────────────────────────────────┐
│ 09:00:30 - Usuário recarrega a página                │
└──────────────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Dashboard    │ │ Analytics    │ │ Connections  │
│ Stats        │ │ KPIs         │ │ Health       │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
   CACHE HIT        CACHE HIT        CACHE HIT
       │                │                │
   5ms              5ms              5ms
       │                │                │
       └────────────────┴────────────────┘
                       │
                Response: 15ms total

🚀 320x MAIS RÁPIDO na segunda carga!

┌──────────────────────────────────────────────────────┐
│ 09:01:00 - Cache de Stats e KPIs expira              │
└──────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────┐
│ 09:01:05 - Usuário navega para Analytics             │
└──────────────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Timeseries   │ │ Funnel       │ │ Campaigns    │
│ Chart        │ │ Analysis     │ │ Metrics      │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
  CACHE MISS       CACHE MISS       CACHE MISS
       │                │                │
  Query DB         Query DB         Query DB
  1200ms           900ms            700ms
       │                │                │
  Save cache       Save cache       Save cache
  TTL: 120s       TTL: 120s        TTL: 120s
       │                │                │
       └────────────────┴────────────────┘
                       │
                Response: 2800ms total

┌──────────────────────────────────────────────────────┐
│ 09:01:30 - Usuário muda granularidade para "week"    │
└──────────────────────────────────────────────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ Timeseries   │
                │ Chart (week) │
                └──────┬───────┘
                       │
                  CACHE MISS
                  (cache key diferente!)
                       │
                  Query DB
                  800ms
                       │
                  Save cache
                  TTL: 120s
                       │
                Response: 800ms

┌──────────────────────────────────────────────────────┐
│ 09:01:45 - Usuário volta para granularidade "day"    │
└──────────────────────────────────────────────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ Timeseries   │
                │ Chart (day)  │
                └──────┬───────┘
                       │
                   CACHE HIT
                   (cache ainda válido!)
                       │
                   5ms
                       │
                Response: 5ms

🎯 Cache inteligente por granularidade!
```

---

### 📱 Cenário: Configuração de Campanha

```
┌──────────────────────────────────────────────────────┐
│ Usuário abre modal para criar campanha WhatsApp      │
└──────────────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Connections  │ │ Templates    │ │ IA Personas  │
│ List         │ │ List         │ │ List         │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
  CACHE MISS       CACHE MISS       CACHE MISS
       │                │                │
  Query DB         Query DB         Query DB
  150ms            400ms            200ms
       │                │                │
  Save cache       Save cache       Save cache
  TTL: 300s       TTL: 900s        TTL: 900s
       │                │                │
       └────────────────┴────────────────┘
                       │
                Response: 750ms

┌──────────────────────────────────────────────────────┐
│ Usuário fecha e reabre o modal (30s depois)          │
└──────────────────────────────────────────────────────┘
                       │
       ┌───────────────┼───────────────┐
       │               │               │
       ▼               ▼               ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ Connections  │ │ Templates    │ │ IA Personas  │
│ List         │ │ List         │ │ List         │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
   CACHE HIT        CACHE HIT        CACHE HIT
       │                │                │
   5ms              5ms              5ms
       │                │                │
       └────────────────┴────────────────┘
                       │
                Response: 15ms

🚀 50x MAIS RÁPIDO!

┌──────────────────────────────────────────────────────┐
│ Usuário cria novo template (via outro modal)         │
└──────────────────────────────────────────────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ POST /api/v1/│
                │ message-     │
                │ templates    │
                └──────┬───────┘
                       │
                  Insere no DB
                       │
                 ⚠️ CACHE NÃO
                   INVALIDADO
                       │
                Response: 200 OK

┌──────────────────────────────────────────────────────┐
│ Usuário reabre modal de criação de campanha          │
└──────────────────────────────────────────────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ Templates    │
                │ List         │
                └──────┬───────┘
                       │
                   CACHE HIT
                   (cache ainda válido)
                       │
                   ❌ Novo template
                   NÃO aparece!
                       │
         Aguardar até 15min (TTL expira)
                  OU
         Recarregar página (força refresh)

⚠️ Trade-off: Performance vs Freshness
💡 Solução futura: Cache invalidation hooks
```

---

## Diagramas de Fluxo

### 🔄 Fluxo Geral de Request → Response

```
┌─────────────────────────────────────────────────────┐
│                  CLIENT REQUEST                      │
│            GET /api/v1/endpoint?params              │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
            ┌──────────────────┐
            │  Next.js Router  │
            │   API Route      │
            └────────┬─────────┘
                     │
                     ▼
          ┌───────────────────────┐
          │   getUserSession()    │
          │  JWT Verification     │
          └────────┬──────────────┘
                   │
         ┌─────────┴─────────┐
         │                   │
         ▼                   ▼
    ✅ Válido          ❌ Inválido
         │                   │
         │                   └──→ 401 Unauthorized
         │
         ▼
┌──────────────────────┐
│  Extract companyId   │
│  from session        │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│        Build Cache Key                        │
│  `endpoint:${companyId}:${params}`           │
└────────┬─────────────────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────────┐
│     Check Cache (EnhancedCache/Redis)         │
│     cache.get(cacheKey)                       │
└────────┬─────────────────────────────────────┘
         │
   ┌─────┴─────┐
   │           │
   ▼           ▼
CACHE HIT   CACHE MISS
   │           │
   │           ▼
   │    ┌──────────────────┐
   │    │  Execute Query   │
   │    │  to Database     │
   │    └────────┬─────────┘
   │             │
   │             ▼
   │    ┌──────────────────┐
   │    │  Transform Data  │
   │    │  (if needed)     │
   │    └────────┬─────────┘
   │             │
   │             ▼
   │    ┌──────────────────┐
   │    │  Save to Cache   │
   │    │  with TTL        │
   │    └────────┬─────────┘
   │             │
   └─────┬───────┘
         │
         ▼
┌──────────────────────┐
│  Return JSON Response │
│  NextResponse.json() │
└────────┬─────────────┘
         │
         ▼
┌──────────────────────┐
│   CLIENT RECEIVES    │
│   DATA (UI UPDATE)   │
└──────────────────────┘
```

### ⏱️ Timeline de Cache Lifecycle

```
T=0s     T=30s    T=60s    T=120s   T=300s   T=600s   T=900s
│        │        │        │        │        │        │
├────────┴────────┴────────┴────────┴────────┴────────┘
│
├─ REAL_TIME (30s)
│  └─ Expires ──┘
│     Cache invalidated
│
├─ MEDIUM (60s)
│  └────────── Expires ──┘
│              Cache invalidated
│
├─ ANALYTICS_CURRENT (60s)
│  └────────── Expires ──┘
│
├─ ANALYTICS_TIMESERIES_CURRENT (120s)
│  └──────────────────── Expires ──┘
│
├─ CONFIG_SEMI_STATIC (300s)
│  └──────────────────────────────────── Expires ──┘
│
├─ ANALYTICS_HISTORICAL (600s)
│  └────────────────────────────────────────────── Expires ──┘
│
└─ CONFIG_STATIC / TIMESERIES_HISTORICAL (900s)
   └────────────────────────────────────────────────────────── Expires ──┘

Legenda:
├─ = Cache ativo
─┘ = Cache expira e é invalidado
```

---

## Performance Esperada

### 📊 Benchmarks Teóricos

#### Dashboard Stats (60s TTL)

```
SEM CACHE:
─────────────────────────────────────────────────────
Request 1:  800ms (query DB)
Request 2:  800ms (query DB)
Request 3:  800ms (query DB)
Request 4:  800ms (query DB)
Request 5:  800ms (query DB)
Request 6:  800ms (query DB)
Request 7:  800ms (query DB)
Request 8:  800ms (query DB)
Request 9:  800ms (query DB)
Request 10: 800ms (query DB)
─────────────────────────────────────────────────────
Total: 8000ms para 10 requests

COM CACHE (60s):
─────────────────────────────────────────────────────
Request 1:  800ms (query DB) → Save cache
Request 2:  5ms   (cache hit)
Request 3:  5ms   (cache hit)
Request 4:  5ms   (cache hit)
...
Request 12: 5ms   (cache hit)
Request 13: 800ms (cache expired, query DB) → Save cache
Request 14: 5ms   (cache hit)
...
─────────────────────────────────────────────────────
Total: ~850ms para 10 requests (em 60s)

🚀 GANHO: 9.4x mais rápido!
💾 Redução de 90% nas queries ao banco
```

#### Analytics Históricos (900s TTL)

```
SEM CACHE:
─────────────────────────────────────────────────────
Usuário visualiza analytics de OUTUBRO:

Request 1 (KPIs):       600ms
Request 2 (Timeseries): 1200ms
Request 3 (Funnel):     900ms
Request 4 (Campaigns):  700ms

Total: 3400ms

Usuário muda granularidade 3x:
Request 5 (day):   1200ms
Request 6 (week):  800ms
Request 7 (month): 1000ms

Total geral: 6600ms
─────────────────────────────────────────────────────

COM CACHE (900s):
─────────────────────────────────────────────────────
Primeira vez (MISS):

Request 1 (KPIs):       600ms → Save cache
Request 2 (Timeseries): 1200ms → Save cache
Request 3 (Funnel):     900ms → Save cache
Request 4 (Campaigns):  700ms → Save cache

Subtotal: 3400ms

Mudanças de granularidade:
Request 5 (day, MISS):   1200ms → Save cache
Request 6 (week, MISS):  800ms → Save cache
Request 7 (month, MISS): 1000ms → Save cache

Subtotal: 3000ms

Segunda visualização (HIT):
Request 8 (day):   5ms
Request 9 (week):  5ms
Request 10 (month): 5ms

Subtotal: 15ms

Total geral: 6415ms (primeira vez) + 15ms (demais vezes)
─────────────────────────────────────────────────────

🚀 GANHO: 440x mais rápido nas visualizações seguintes!
💾 Dados históricos cacheados por 15 minutos
```

### 💰 Impacto em Custos de Infraestrutura

```
Cenário: 100 usuários simultâneos, cada um acessa dashboard 1x/min

SEM CACHE:
─────────────────────────────────────────────────────
100 users × 60 requests/hora = 6000 requests/hora
6000 requests × 800ms = 4,800,000ms = 1333 segundos de DB
Database CPU: ~22 min/hora em queries (36% utilization)
─────────────────────────────────────────────────────

COM CACHE (60s TTL):
─────────────────────────────────────────────────────
Cache hit rate: ~95% (dentro de 60s)
6000 requests × 5% MISS = 300 queries reais
300 queries × 800ms = 240,000ms = 66 segundos de DB
Database CPU: ~1 min/hora em queries (1.8% utilization)
─────────────────────────────────────────────────────

🎯 ECONOMIA:
- 95% menos carga no banco
- 20x menos CPU utilizado
- Suporta 20x mais usuários com mesma infra
- Redução de custos de ~60-80% em hosting
```

---

## 🔑 Key Takeaways

### ✅ O Que Foi Implementado

1. **Sistema de cache tiered** com 4 níveis de TTL
2. **21 endpoints cobertos** (vs 12 anteriores)
3. **Cache keys únicos** por tenant + parâmetros
4. **TTL condicional** (histórico vs atual)
5. **Multi-tenant security** preservada

### 🎯 Principais Benefícios

1. **Dashboard 10x mais rápido** (800ms → 5ms)
2. **Analytics históricos 440x mais rápidos**
3. **90% menos queries** ao banco de dados
4. **Suporte a 20x mais usuários** simultâneos
5. **Redução de 60-80% em custos** de infraestrutura

### ⚠️ Limitações Atuais

1. **Sem invalidação automática** de cache
   - Mudanças nos dados não atualizam cache imediatamente
   - Solução: Aguardar TTL expirar ou recarregar página

2. **TTLs fixos** (não adaptativos)
   - Cache não se ajusta baseado em padrões de uso
   - Solução futura: TTL dinâmico baseado em hit rate

3. **Sem métricas de observabilidade**
   - Não rastreamos hit/miss rate em produção
   - Solução futura: Dashboard de métricas de cache

### 🚀 Próximos Passos Recomendados

1. **Cache Invalidation Hooks**
   ```typescript
   // Invalidar cache ao criar/editar
   await createTemplate(data)
   await invalidateCache(`message-templates:${companyId}:*`)
   ```

2. **Observabilidade**
   ```typescript
   // Métricas de performance
   cacheHitRate.record(hit ? 1 : 0)
   cacheLatency.record(responseTime)
   ```

3. **Expansão Gradual**
   - Adicionar cache aos próximos 10 endpoints
   - Cobrir 50% dos endpoints mais acessados

---

## 📚 Referências Técnicas

- **Enhanced Cache:** `src/lib/enhanced-cache.ts`
- **API Cache:** `src/lib/api-cache.ts`
- **TTL Constants:** `src/lib/api-cache.ts` (CacheTTL enum)
- **Endpoints:** `src/app/api/v1/**/route.ts`

---

**Documentação gerada em:** 20/11/2024  
**Versão do Sistema:** 2.4.1  
**Cache Coverage:** 21/151 endpoints (14%)
